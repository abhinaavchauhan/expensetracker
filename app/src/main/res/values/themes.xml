<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Theme — uses DayNight for automatic light/dark switching -->
    <style name="Theme.ExpenseTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:windowLightStatusBar">@bool/is_light_status_bar</item>
        <item name="android:navigationBarColor">@color/nav_bar_bg</item>
        <item name="android:windowLightNavigationBar">@bool/is_light_status_bar</item>
        <item name="android:textColorPrimary">@color/text_primary_dark</item>
        <item name="android:textColorSecondary">@color/text_secondary_dark</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <!-- Splash Theme (always dark for premium feel) -->
    <style name="Theme.ExpenseTracker.Splash" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
    </style>

    <!-- Window Transitions -->
    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <!-- Card Style (auto-adapts via semantic colors) -->
    <style name="ExpenseCard">
        <item name="cardCornerRadius">@dimen/corner_large</item>
        <item name="cardElevation">0dp</item>
        <item name="cardBackgroundColor">@color/background_card_dark</item>
        <item name="contentPadding">@dimen/card_padding</item>
    </style>

    <!-- Button Styles -->
    <style name="PrimaryButton" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/primary</item>
        <item name="cornerRadius">@dimen/corner_medium</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textSize">@dimen/text_subtitle</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
    </style>

    <style name="SecondaryButton" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:textColor">@color/primary</item>
        <item name="strokeColor">@color/primary</item>
        <item name="cornerRadius">@dimen/corner_medium</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Text Styles (all reference semantic colors) -->
    <style name="TextDisplay">
        <item name="android:textSize">@dimen/text_display</item>
        <item name="android:fontFamily">sans-serif-bold</item>
        <item name="android:textColor">@color/text_primary_dark</item>
    </style>

    <style name="TextHeadline">
        <item name="android:textSize">@dimen/text_headline</item>
        <item name="android:fontFamily">sans-serif-bold</item>
        <item name="android:textColor">@color/text_primary_dark</item>
    </style>

    <style name="TextTitle">
        <item name="android:textSize">@dimen/text_title</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textColor">@color/text_primary_dark</item>
    </style>

    <style name="TextSubtitle">
        <item name="android:textSize">@dimen/text_subtitle</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textColor">@color/text_secondary_dark</item>
    </style>

    <style name="TextBody">
        <item name="android:textSize">@dimen/text_body</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textColor">@color/text_secondary_dark</item>
    </style>

    <style name="TextCaption">
        <item name="android:textSize">@dimen/text_caption</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textColor">@color/text_tertiary_dark</item>
    </style>

    <!-- Amount Large -->
    <style name="TextAmountLarge">
        <item name="android:textSize">@dimen/text_amount_large</item>
        <item name="android:fontFamily">sans-serif-bold</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Bottom Sheet Style (theme-aware) -->
    <style name="BottomSheetDialog" parent="Theme.Material3.DayNight.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Material3.BottomSheet">
        <item name="shapeAppearanceOverlay">@style/BottomSheetShapeOverlay</item>
        <item name="backgroundTint">@color/bottom_sheet_bg</item>
    </style>

    <style name="BottomSheetShapeOverlay">
        <item name="cornerSizeTopLeft">@dimen/corner_xlarge</item>
        <item name="cornerSizeTopRight">@dimen/corner_xlarge</item>
    </style>

    <!-- Profile Icon Style -->
    <style name="CircleProfileImage">
        <item name="cornerSize">50%</item>
    </style>

    <!-- Chip Style (theme-aware) -->
    <style name="FilterChip" parent="Widget.Material3.Chip.Filter">
        <item name="chipCornerRadius">@dimen/corner_round</item>
        <item name="chipBackgroundColor">@color/chip_bg_selector</item>
        <item name="android:textColor">@color/chip_text_selector</item>
        <item name="checkedIconVisible">false</item>
        <item name="chipStrokeWidth">0dp</item>
    </style>

    <!-- Tab Style (theme-aware) -->
    <style name="AnalyticsTab" parent="Widget.Material3.TabLayout">
        <item name="tabIndicatorColor">@color/primary</item>
        <item name="tabSelectedTextColor">@color/primary</item>
        <item name="tabTextColor">@color/text_secondary_dark</item>
        <item name="tabBackground">@color/transparent</item>
        <item name="tabIndicatorFullWidth">false</item>
        <item name="tabRippleColor">@color/ripple_dark</item>
    </style>
    <!-- Bottom Nav Custom Indicator -->
    <style name="App.CustomIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/active_indicator_color</item>
        <item name="height">32dp</item>
    </style>
</resources>
